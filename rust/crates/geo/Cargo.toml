[package]
edition.workspace = true
name = "longbridge-geo"
version.workspace = true
description = "Longbridge geo-detection helper (CN vs global)"
license = "MIT OR Apache-2.0"

[dependencies]
reqwest = { workspace = true, features = ["rustls-tls"] }
